#include <stdio.h>

int main()
{
    char name[30];
    int roll;

    printf("Enter your name: ");
    scanf("%s", name);

    printf("Enter roll number: ");
    scanf("%d", &roll);

    printf("\nName: %s\n", name);
    printf("Roll Number: %d\n", roll);

    return 0;
}